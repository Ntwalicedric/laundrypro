# WhatsApp Cloud API Configuration
# Required: Your Meta WhatsApp Phone Number ID
WHATSAPP_PHONE_NUMBER_ID=

# Required: Your Meta WhatsApp Access Token
# Get this from: Meta Business Suite → WhatsApp → API Setup
# Or: Meta for Developers → Your App → WhatsApp → API Setup
WHATSAPP_ACCESS_TOKEN=your_access_token_here

# Required: The dry cleaner's WhatsApp number (where orders will be sent)
# Format: digits only, no + or spaces
DRY_CLEANER_WHATSAPP_NUMBER=

# Optional: WhatsApp API Version (defaults to v18.0)
WHATSAPP_API_VERSION=v18.0

# Optional: CORS allowed origins (defaults to localhost)
ALLOWED_ORIGINS=http://localhost:8080,http://127.0.0.1:8080

# Optional: Body size limits (defaults shown)
JSON_BODY_LIMIT=1mb
URLENCODED_BODY_LIMIT=1mb

